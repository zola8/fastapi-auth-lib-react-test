type StatusType = 'success' | 'error' | ''

interface StatusState {
  type: StatusType
  message: string
}type StatusType = 'success' | 'error' | ''
export interface StatusState {
  type: StatusType
  message: string
}

